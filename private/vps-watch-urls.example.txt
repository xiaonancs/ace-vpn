# ace-vpn · 可选：在 speed-test 默认列表之外再监控的 URL（每行一个 https?://）
#
# vps-watch-urls.sh 会默认合并：
#   scripts/speed-test-endpoints.txt（与 speed-test.sh 里 AI/Cursor/媒体/国内对照一致）
#   + 本文件（若存在 private/vps-watch-urls.txt）
# 去重后推到各 VPS 探测。
#
# 不需要额外地址时：可不建 vps-watch-urls.txt，仅跑 speed-test 全集即可。

https://example.com/
